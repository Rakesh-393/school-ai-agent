# Versions below are the ones this project was built and tested against
# (Python 3.13, Windows). Loosen the pins once you know it works for you.

# --- web + db ---
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.52
python-dotenv==1.2.3

# SQL Server, used only when DB_BACKEND=mssql. Harmless to install otherwise.
# It needs the Microsoft ODBC Driver for SQL Server on the machine as well --
# the pip package is only the Python binding, not the driver:
# https://learn.microsoft.com/sql/connect/odbc/download-odbc-driver-for-sql-server
pyodbc==5.3.0

# --- llm providers ---
# The openai package is used purely as a TRANSPORT for any OpenAI-compatible
# endpoint: Groq, Google Gemini, Ollama, OpenRouter, Together...
openai==3.6.0
anthropic>=0.69.0        # only needed when LLM_PROVIDER=anthropic

# --- fuzzy entity matching ---
RapidFuzz==3.14.6

# --- rag ---
chromadb==1.5.9          # bundles onnxruntime + all-MiniLM-L6-v2; no PyTorch needed
pypdf==6.16.2            # so you can drop PDF circulars into data/docs/
